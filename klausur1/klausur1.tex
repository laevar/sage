\documentclass[a4paper,12pt,DIV15]{scrartcl}
\input{../klausur_header}

\begin{aufg}[3]
Welche verschiedenen Möglichkeiten kennen sie Schleifen zu konstruieren und worin unterscheiden sich diese? 
\end{aufg}

\begin{aufg}[3]
Was sind Dictionaries und worin besteht ihr wesentlicher Vorteil sowie Nachteil im Vergleich zu den anderen Datentypen?
\end{aufg}

\begin{aufg}[3]
Worin besteht das Problem bei Gleitkommazahlen im Computer? Fassen sie kurz zusammen welche Möglichkeiten es gibt diese Probleme zu umgehen.
\end{aufg}

\begin{aufg}[4]
Schreiben zwei unterschiedliche Funktionen mit \isage{def}, die folgende Funktion berechnet: 
\[ f(s) = \sum_{t=0}^s e^{-t} \sin( t^2)  \]
Die eine Funktion soll \isage{sum()} benutzen, die andere nicht.
\end{aufg}


\begin{aufg}[4]
Gegeben sei der Vektor \isage{xvec = [x0,...,xn]} für $n\in \mathbb{N}$.
Mit welcher Befehlsfolge kann man die Vandermonde-Matrix 
\[ 
 V:= \left(\begin{array}{ccccc} 
 1 & x_0 & x_0^2 & \hdots & x_0^{n}\\
 1 & x_1 & x_1^2 & \hdots & x_1^{n}\\
 \vdots & \vdots & \vdots & \vdots & \vdots\\
 1 & x_n & x_n^2 & \hdots & x_n^{n}\\
 \end{array} \right) 
 \]
erstellen?
\end{aufg}
\clearpage
\begin{aufg}[4]
Schreiben Sie eine rekursive Funktion \isage{verfeinere(f, a, b, TOL)}, die mit gegebener Funktion $f$, Intervall $[a,\, b]$ und 
Genauigkeit $TOL$ Stützstellen bestimmt, so dass sich die Funktionswerte an benachbarten Stützstellen um weniger 
als $TOL$ unterscheiden. Gehen Sie dabei folgendermaßen vor:
\begin{itemize}
\item Wenn $|f(a)-f(b)| < TOL$ ist, geben Sie die Liste $[a,\, b]$ zurück.
\item Andernfalls nutzen Sie die Funktion rekursiv, um jeweils die linke 
Intervall-Hälfte $[a,\,\frac{a+b}{2}]$ und die rechte Intervall-Hälfte $[\frac{a+b}{2},\, b]$ zu verfeinern. 
Fügen Sie die Resultate in eine Liste zusammen und geben Sie diese anschließend als Ergebnis zurück.
\end{itemize}
\end{aufg}

\end{document}

