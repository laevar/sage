\documentclass[a4paper,12pt,DIV15]{scrartcl}
\input{../klausur_header}


%-----------------------------------------------------------------------------------
% \begin{aufg}{4}
% \begin{enumerate}
% \item Sei $a=1234$ im Dezimalsystem. 
%     Berechnen Sie für $a$ die Darstellung zur Basis $5$.
% \item  Sei $1101$ die Darstellung einer Zahl zur Basis $2$. Berechnen Sie die Darstellung dieser Zahl zur Basis 3.
% \end{enumerate}
% \end{aufg}
%-------------------------------------------------------------------------------------
% \begin{aufg}{6}
% Zu welchen Auswertungen führt der Aufruf von \isage{x1}, \ldots, \isage{x6} nach
% den folgenden Eingaben.
% \begin{enumerate}
% \item 
% \verb~a:=2: b:=2: c:=2: x1:=a+b+c: x1~
% \item 
% \isage{L:=[1,2,3]: M:=[2,3,4]: x2:= L.M: x2}
% \item 
% \verb~k:=r+s: m:=r-s: x3:=k+m: x3~ 
% \item 
% \verb~U:=brat: V:=wurst: x4:=bratwurst: x4~ 
% \item 
% \verb~e:=PI: f:=sin: x5:=f(e): x5~ 
% \item 
% \verb~R:={4,2,1,3,4,2}: x6:=nops(R): x6~
% \end{enumerate}
% \end{aufg}

\begin{aufg}{3}
Schreiben sie eine Funktion mit \isage{def}, die 
\[ f(s) = \int_0^1 e^{-s t} \sin( t^2) dt \]
numerisch approximiert. 
\end{aufg}

\begin{aufg}{3}
 Was sind die Unterschiede zwischen Listen, Tuple und Dictionaries ? Geben sie auch
jeweils ein Beispiel an, welches den Unterschied hervorhebt
\end{aufg}


\begin{aufg}{3}
Mit welcher Befehlsfolge kann man die Vandermonde-Matrix
 
\[ 
V:= \left(\begin{array}{ccccc} 
1 & x_1 & x_1^2 & \hdots & x_1^{n-1}\\
1 & x_2 & x_2^2 & \hdots & x_2^{n-1}\\
\vdots & \vdots & \vdots & \vdots & \vdots\\
1 & x_n & x_n^2 & \hdots & x_n^{n-1}\\
\end{array} \right) 
\]
erstellen?
\end{aufg}

%-------------------------------------------------------------------------------------
% \begin{aufg}{4}
% Die folgenden Eingaben sind fehlerhaft: Erklären Sie jeweils mit einem kurzen Stichwort die
% Fehlermeldung.
% \begin{enumerate}
% \item \verb~L:=[1,2,3]; M:=[2,3,4]; L union M~
% \item \verb~a:=1, b:=1: a+b~
% \item \verb~E:=matrix([[1,2],[3,4]]); F:=matrix([2,3]): E*F;~
% \item \verb~U:=[1,2,3,4]: delete U[4]: U[4]=3~
% \end{enumerate}
% \end{aufg}
%-----------------------------------------------------------------------------------
% \begin{aufg}{8}
% Geben Sie an, wie Sie die folgenden Objekte in MuPAD
% definieren können:
% \begin{enumerate}
% \item 
% $\lim_{n \rightarrow \infty} \frac{5}{n}$
% \item 
% $\begin{pmatrix}
%         1&2&3\\
% 	4&5&0
%        \end{pmatrix}$
% \item 
% \begin{tabular}{c|c}\hline
% a&2\\
% b&3\\
% 4&1
% \end{tabular}
% \item $\sum_{a=2}^{n} \log_b(a)$
% \item $\prod_{i=2}^k \frac{(2i-1)(2i+1)}{2i(2i-2)}$
% \item Schaubild des Graphs der Funktion $f(x) = x^2 - 2$ für $-3\leq x \leq 6$
% \item $\{x \in \N\;|\; 0 \leq x \leq 100 \text{ und } 2 \text{ teilt } x\}$ 
% \item $\int_1^{100} \sin(xy) \mathop{dy}$
% \end{enumerate}
% \end{aufg}

\begin{aufg}{5}
Schreiben sie eine Funktion mit Input-Variablen $x_0$ und $TOL$, die die Folge
\[ x_{n+1} = x_n - \frac{x_n^2-5}{2 x_n}, \quad n \in \mathbb{N} \]
berechnet und abbricht, wenn $|x_{n}-x_{n-1}| \leq TOL$ ist. Die Funktion soll
$x_{n}$ und das zugeh\"orige $n$ zur\"uckgeben. 
\end{aufg}

\begin{aufg}{2}
Kurvendiskussion?
\end{aufg}

%-------------------------------------------------------------------------
\begin{aufg}{2}
Welche Möglichkeiten stehen zur Verfügung um die Gleichheit bei einer Gleichung des Typs 
\isage{<Ausdruck1> == <Ausdruck2>} zu überprüfen ?
\end{aufg}
%-------------------------------------------------------------------------
% \begin{aufg}{2}
% Schreiben Sie eine MuPAD-Prozedur \isage{programm1} die für eine Zahl $n>10$ die Zeichenkette \isage{MuPAD ist super} und für $n\leq10$ die Zeichenkette   \isage{MuPAD ist Mist} zurückgibt (Typenüberprüfung braucht nicht durchgeführt zu werden).
% \end{aufg}
%-----------------------------------------------------------------------------------
\begin{aufg}{4}
Schreiben Sie ohne Verwendung der Funktion \isage{max} eine MuPAD-Prozedur\\ \isage{programm2}, die aus einer Liste von Zahlen die größte Zahl zurückliefert (Typenüberprüfung braucht nicht durchgeführt zu werden).
\end{aufg}
\bigskip

% %-------------------------------------------------------------------------
% Bei den nächsten aufgn, kreuzen Sie jeweils an.
% Falsche Antworten werden mit $-1$ Punkt bewertet. Richtige Antworten
% werden mit $+1$ Punkt bewertet. Enthaltungen, falls beide
% Möglichkeiten angekreuzt sind, oder falls keine Zuordnung erkennbar
% ist, werden mit null Punkten bewertet.  Insgesamt können nicht weniger
% als null Punkte erreicht werden.
% %--------------------------------------------------------
% \begin{aufg}{4}
% \begin{tabular}[t]{m{11cm}cc}
%     &Ja& Nein\\
%    Es gibt in MuPAD einen Datentyp für die ganzen Zahlen $\Z$. &$\Box$& $\Box$\\[2ex]
%    Es gibt in MuPAD einen Datentyp für die rationalen Zahlen $\Q$.  &$\Box$& $\Box$\\[2ex]
%    Funktionen und Prozeduren haben denselben Datentyp.  &$\Box$& $\Box$\\[2ex]
%    Die Eingabe von $2$ und $2.0$ bei einer Funktion ist gleichbedeutend. &$\Box$& $\Box$
% \end{tabular}
% \end{aufg}
% %--------------------------------------------------------------------------
% \begin{aufg}{4}
% \begin{tabular}[t]{m{11cm}cc}
%     &Ja& Nein\\
%     Die Auswertung von Bezeichnern kann sich zur Laufzeit ändern.&$\Box$& $\Box$\\[2ex]
%     MuPAD rechnet bei symbolischen Größen, wie z.B. $\sqrt{2}$ intern mit Gleitkommazahlen.&$\Box$& $\Box$\\[2ex]
%     Die Konstante \isage{DIGITS} kann nicht verändert werden.&$\Box$& $\Box$\\[2ex]
%     Die Auswertungstiefe eines Bezeichners kann durch MuPAD-Funktionen und -Konstanten beeinflusst werden.
%     &$\Box$ &$\Box$
% \end{tabular}
% \end{aufg}
% %------------------------------------------------------------------------
% \begin{aufg}{4}
% \begin{tabular}[t]{m{11cm}cc}
%     &Ja& Nein\\
%     Innerhalb einer Tabelle können gleiche Indizes mehr als einmal vorkommen.&$\Box$& $\Box$\\[2ex]
%     Die Funktion \isage{linalg::eigenvalues} berechnet alle Eigenvektoren einer Matrix.&$\Box$& $\Box$\\[2ex]
%     Der Ausdruck \verb~a+b~ ist ein zulässiger Index einer Tabelle.&$\Box$& $\Box$\\[2ex]
%     Mit der Funktion \isage{map} können Funktionen auf die Einträge einer Matrix angewendet werden.&$\Box$& $\Box$
% \end{tabular}
% \end{aufg}
%------------------------------------------------------------------------
\end{document}

